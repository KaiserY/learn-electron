{
    "scripts": {
        "start": "npm run compile && electron ./app",
        "postinstall": "install-app-deps",
        "clean:dist": "rimraf dist",
        "clean:out": "rimraf app/public/out",
        "compile": "npm run clean:out && tsconfig -i 2 && tsc",
        "pack": "npm run compile && build",
        "dist": "npm run compile && build"
    },
    "build": {
        "prune": true,
        "download": {
            "mirror": "https://npm.taobao.org/mirrors/electron/",
            "strictSSL": false
        },
        "linux": {
            "target": "rpm",
            "depends": []
        },
        "win": {
            "msi": false
        }
    },
    "devDependencies": {
        "cpr": "^1.1.1",
        "devtron": "^1.1.2",
        "electron-builder": "^3.26.2",
        "electron-prebuilt": "^1.1.3",
        "rimraf": "^2.5.2",
        "tsconfig-glob": "^0.4.3",
        "typescript": "^1.8.10"
    }
}
